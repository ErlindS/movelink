# Trigger-Kommentar zur Überprüfung der Workflow-Schreibrechte
name: Continuous Documentation

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs_site/**'
  workflow_dispatch:

permissions:
  contents: write

#jobs:
#  build-docs:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Code auschecken
#        uses: actions/checkout@v3
#
#      - name: Python einrichten
#        uses: actions/setup-python@v4
#        with:
#          python-version: '3.10'
#
#      - name: Abhängigkeiten installieren
#        run: |
#          python -m pip install --upgrade pip
#          pip install reportlab
#
#      - name: Dokumentation & PDF generieren
#        run: |
#          python scrape_docs.py
#          python generate_pdf.py
#
#      - name: Änderungen zurück ins Repository pushen
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        run: |
#          git config --local user.email "action@github.com"
#          git config --local user.name "GitHub Action"
#          git add docs_site/
#          git commit -m "docs: auto-generate documentation dashboard and PDF [skip ci]" || echo "Keine Änderungen zu committen"
#          git push https://x-token-access:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git HEAD:${{ github.ref_name }}

