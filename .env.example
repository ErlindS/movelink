# Copy this file to .env before running docker compose:
#   cp .env.example .env

# ── AP1 Frontend ──────────────────────────────────────────────────────────────
EXPO_PUBLIC_API_URL=http://localhost:3000
EXPO_PUBLIC_WS_URL=ws://localhost:3000/ws

# ── AP2 Backend / Database (uncomment once backend is ready) ──────────────────
# DATABASE_URL=postgres://movelink:movelink@db:5432/movelink
# POSTGRES_USER=movelink
# POSTGRES_PASSWORD=movelink
# POSTGRES_DB=movelink
